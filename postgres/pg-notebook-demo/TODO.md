* Finish python code to display tables + columns (navigating the relationships through each entity)
* Add code to make a change to postgres table
* Issue rest call to force refresh connector
* Repeat code to display, demonstrating difference

Later:
* Integrate new notebook into base demos (requires changes on optional connectors ++ - tbd)
